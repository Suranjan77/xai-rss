# Copy to .env for docker compose. These point the containerized app at the
# host's GPU services (the LLM and the TTS server), which run on the host.
# With network_mode: host, 127.0.0.1 reaches the host.

IDIGEST_LLM_BASE_URL=http://127.0.0.1:8080
IDIGEST_TTS_URL=http://127.0.0.1:8090
